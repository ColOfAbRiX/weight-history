# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# API
GET     /weights/all                controllers.DataController.getAll(start: Option[Long] ?= None, end: Option[Long] ?= None)
GET     /weights/weekly             controllers.DataController.getWeekly(start: Option[Long] ?= None, end: Option[Long] ?= None)
PUT     /weights/add                controllers.DataController.add()
POST    /weights/modify/:id         controllers.DataController.modify(id: Long)
DELETE  /weights/remove/:id         controllers.DataController.remove(id: Long)

# Web UI
GET     /weights                    controllers.HomeController.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
